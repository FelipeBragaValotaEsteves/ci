{
  "name": "users-api",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "initdb": "node src/initDatabase.js",
    "migrate": "npm run initdb && knex migrate:latest",
    "rollback": "knex migrate:rollback",
    "dev": "npm run initdb && node src/server.js",
    "test": "NODE_ENV=test jest --runInBand"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "knex": "^3.1.0",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}